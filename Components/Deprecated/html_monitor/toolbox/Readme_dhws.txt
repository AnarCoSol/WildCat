dhws need the automated set of directory (if python /root/etc it doesn't found itself to run a recursive istance)
dhws output should be more simplified or, at last line, print a message such as "node x is up", or "node x is down"
